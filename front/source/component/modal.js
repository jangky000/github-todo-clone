export default class {
    render() {
        let divs = '';
        divs += `<div class="modal">`;
        divs += `<div class="modal_overlay"></div>`;
        divs += `<div class="modal_content">`;
        divs += `모달`;
        divs += `</div>`;
        divs += `</div>`;
        return divs;
    }
    addEvent() {
        //
    }
}
